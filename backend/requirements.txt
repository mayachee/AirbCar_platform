# Python 3.9+ required (compatible with Render)

django==4.2.21
djangorestframework==3.14.0
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.2.0
psycopg[binary]==3.2.13
Pillow==10.4.0
gunicorn==22.0.0
gevent==24.11.1  # Async workers for 3x more concurrent users (24.11.1+ required for Python 3.13)
python-dotenv==1.0.1
whitenoise==6.6.0
supabase==2.6.0
httpx[http2]
requests==2.31.0
setuptools>=56.0.0

# ===== TESTING DEPENDENCIES =====
schemathesis==3.28.0  # 🔥 AUTOMATIC API TESTING - Just run it, no code needed!
drf-spectacular==0.27.0  # OpenAPI schema generation

# Optional: Redis for production caching (requires external Redis service)
# django-redis==5.4.0 